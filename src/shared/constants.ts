export const STORAGE_KEYS = {
	annictAccessToken: "tamate.annict.accessToken",
	lastVisitedPath: "tamate.navigation.lastVisitedPath",
	pendingAuthPath: "tamate.navigation.pendingAuthPath",
} as const;
